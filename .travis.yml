sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t shaibad/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shaibad/docker-react npm run test